# ThemeShop

shop bán phần mềm

## Cài đặt

1. Cài Node.js (>=18).
2. Cài các phụ thuộc npm:

   ```bash
   npm install
   ```

3. Khởi động server tĩnh để xem giao diện:

   ```bash
   npm run start
   ```

   Mặc định dự án chạy tại `http://localhost:3000`.
